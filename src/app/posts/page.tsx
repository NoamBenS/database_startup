import { auth, currentUser } from "@clerk/nextjs/server";

export default function Home() {
    // const { userId } = auth();

    return (
        <main>
            {/* {userId} */}
        </main>
      );
    }
    